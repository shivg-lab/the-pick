export type EventWeather = { status: 'available' | 'outside_window' | 'unavailable' | 'disabled'; message: string; source_url: string; retrieved_at: string | null; window_start: string | null; window_end: string | null; temperature_min_f: number | null; temperature_max_f: number | null; feels_like_min_f: number | null; feels_like_max_f: number | null; rain_probability_max: number | null; wind_max_mph: number | null };
export type Evidence = { id: string; claim: string; category: string; source_id: string; source_title: string; source_url: string; authority: string; retrieved_at: string; freshness: string; confidence: number; field: string | null };
export type Event = { id: string; name: string; sport: string; league: string; level: string; teams: string[]; start_time: string; venue: string; city: string; price_min: number | null; price_max: number | null; outing_extra: number | null; ticket_url: string; data_mode: string; signals: string[]; color: string; retrieved_at: string };
export type Recommendation = { event: Event; rank: number; score: { opportunity_score: number; evidence_confidence: number; factors: Record<string, number | null>; weights: Record<string, number>; rationale: Record<string, string>; missing_factors: string[] }; distance_miles: number | null; estimated_total: number | null; explanation: string; why_it_matters: string[]; why_it_fits: string[]; tradeoffs: string[]; guide: { policy: { fields: Record<string, Evidence>; conflicts: string[]; warnings: string[]; last_verified: string | null }; food: { vendor: string; location: string | null; vegetarian: boolean | null; vegan: boolean | null; gluten_free: boolean | null; allergen_information: string; evidence: Evidence }[]; arrival: string[]; parking?: Evidence[]; seating?: Evidence[]; attire_tips?: string[]; attire_rules?: Evidence[]; weather?: EventWeather; seating_tip?: string; warnings: string[] }; citations: Evidence[]; warnings: string[] };
export type Activity = { step: string; status: string; tool: string | null };
export type Results = { request_id: string; parsed_intent: { budget_type: 'total' | 'per_ticket'; sports: string[]; location: string; adults: number; children: number; budget: number | null; radius_miles: number | null; dietary: string[]; atmosphere: string; additional_constraints: string[] }; candidates_considered: number; exclusions: { event_id: string; name: string; reason: string }[]; recommendations: Recommendation[]; baseline: { event_id: string; name: string; distance_miles: number | null; qualifies: boolean; personalized_rank: number | null }[]; activity: Activity[]; data_mode: string; agent_active: boolean; retrieval_mode: string; provider_status: Record<string, unknown>; warnings: string[]; processing_ms: number; created_at: string };
export type Providers = { agent_ready: boolean; vector_ready: boolean; documents: number; chat_model: string; embedding_model: string; app_mode: string; ollama: { reachable: boolean; chat_installed: boolean; embedding_installed: boolean }; setup: string[] };
export type Filters = { location: string; date_from?: string | null; date_to?: string | null; radius_miles: number | null; sports: string[]; teams: string[]; budget: number | null; budget_type: 'total' | 'per_ticket'; adults: number; children: number; family_friendly: boolean; atmosphere: string; local_openness: boolean; dietary: string[]; dietary_required: boolean; accessibility: string[]; first_time: boolean };
